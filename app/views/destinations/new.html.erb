<%= form_for [@itinerary, @destination] do |f| %>
<%= render partial: '/itineraries/destination_fields', locals: { f: f } %>
<%= f.submit "Create itinerary", class: "background-sand border-0 position-itinerary-button" %>
<% end %>
    
  
