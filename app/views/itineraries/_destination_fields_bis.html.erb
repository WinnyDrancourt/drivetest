<div class ='columns'>
  <p class="text-center mb-0">City</p>
  <%= f.text_field :city, class: "text-center w-100"  %>
  <p class="text-center mb-0">Notes</p>
  <%= f.text_area :notes, class: "text-center w-100"  %>
  <p class="text-center mb-0">Duration</p>
  <%= f.number_field :staying_time, in: 1..31, step: 1, class: "text-center w-100"%>
  <!-- Remove ? but dont work -->
  <%# link_to_remove_nested(f, link_text: 'Destroy', fields_wrapper_selector: '.columns') %>
  <%#if params[:dynamic_form] == '1' %>
    <%# link_to_remove_nested(f, link_text: 'Destroy', fields_wrapper_selector: '.columns') %>
  <%#else %>
    <%#if f.object.persisted? %>
      <%# f.input :_destroy, as: :boolean, label: 'Destroy' %>
    <%#end %>
  <%#end %>
</div>
